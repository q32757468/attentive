# Attentive for VS Code

This workspace extension injects a window-specific Attentive callback URI into newly created or restarted integrated terminals.

After installing the local VSIX, create or restart the terminal before invoking `attentive`. Existing terminals are not modified.

Run **Attentive: Show VS Code Callback Status** to see whether injection succeeded. The command deliberately does not reveal the callback URI.
